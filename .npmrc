# Keep installs strict so published libraries never rely on phantom deps.
shamefully-hoist=false
link-workspace-packages=true
# Tooling (tsup/vitest/etc.) lives at the workspace root; allow package scripts to resolve it.
prefer-workspace-packages=true
