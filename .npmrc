# Workspace internal deps use the explicit `workspace:*` protocol, so no
# link/hoist settings are needed here. Kept minimal so plain `npm` commands
# (e.g. `npm login`, `npm publish`) don't warn about pnpm-only config keys.
